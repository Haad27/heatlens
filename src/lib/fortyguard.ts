/**
 * Barrel for the FortyGuard client.
 * Implementation lives in `./fortyguard/client.ts` so types, the HTTP client
 * and the mock simulator can stay in separate modules.
 */
export * from "@/lib/fortyguard/client";
export * from "@/lib/fortyguard/types";
